{
    "name": "repo-hunt",
    "version": "1.0.0",
    "description": "package for creating workers templates",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "Seth <sethalexander@pm.me>",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.18.2"
    },
    "dependencies": {
        "serverless-cloudflare-workers": "^1.2.0"
    }
}
